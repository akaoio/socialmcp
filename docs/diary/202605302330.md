# 2026-05-30 23:30 — Drop ZEN

## Quyết định

Gỡ hoàn toàn ZEN khỏi code và doc. Lý do: ZEN chưa thực sự cần thiết ở giai đoạn này — transport giữa MCP server và extension vẫn chưa được implement, và việc giữ ZEN scaffolding (relay, keypair, wasm, CSP `wasm-unsafe-eval`, env vars) chỉ tạo drift và khiến người mới nhầm tưởng luồng đã hoạt động. Khi nào cần một transport, sẽ chọn lại từ đầu (có thể là native messaging, fetch polling, hoặc lại là ZEN — không cần quyết bây giờ).

## Thay đổi

### Code
- `src/server/bridge.js`: thay bằng stub `class Bridge { send() { throw 'no transport yet' } }`. Server vẫn start được, mọi MCP tool ném lỗi rõ ràng.
- `src/server/index.js`: bỏ comment ZEN trong header.
- `package.json`: gỡ `dependencies.@akaoio/zen`. Project về zero runtime deps.
- `build.js`: gỡ `zenServiceStub`, `inlineDynamicImports`, copy `pen.wasm`/`crypto.wasm`. Bỏ `ws://127.0.0.1/*` + `http://127.0.0.1/*` khỏi `host_permissions` auto-gen.
- `src/browser/manifest.json`: bỏ `wasm-unsafe-eval` khỏi CSP, bỏ `ws://` + `http://` host_permissions.
- `.env.example`: xóa file (không còn env var nào cần).
- `.gitignore`: bỏ comment nhắc tới SOCIALMCP_SECRET.

### Docs
- `README.md`: kiến trúc diagram bỏ ZEN, thay bằng "transport chưa implement". Bảng env vars thành "không cần biến nào". Bỏ `@akaoio/zen` khỏi `npm install` note. Warning footer cập nhật.
- `.github/copilot-instructions.md`: gỡ ZEN ở mọi nơi (architecture diagram, bridge section, build system, env vars, testing).
- `docs/plugin-dev-guide.md`: section "MCP-server-to-extension ZEN bridge" → "MCP-server-to-extension transport"; mô tả transport-agnostic, chỉ rõ 2 file cần đổi khi pick transport sau này (`bridge.js` + `background/index.js`).

### Giữ nguyên
- Diary entries cũ (`202605291900.md` v.v.) còn nhắc tới ZEN — đây là lịch sử, không sửa.

## Verify
- `npm uninstall @akaoio/zen` → sạch.
- `npm run build` → green, không cần wasm, không cần stub.
- `grep -r zen|ZEN|@akaoio` → chỉ còn ở diary entries cũ và 1 dòng explanation trong copilot-instructions ("Previous SOCIALMCP_PORT/SECRET belonged to a removed transport").

## Hệ quả

- Project về zero runtime deps; chỉ còn devDependencies (rollup + plugins).
- Extension CSP đơn giản hơn (không cần `wasm-unsafe-eval`).
- Khi cần transport, có một điểm rõ ràng để bắt đầu: `src/server/bridge.js` stub có comment chỉ dẫn.
