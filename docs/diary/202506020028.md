# 2026-06-02 00:28 — Docs audit and cleanup

## What changed

### Dead code removed
- `src/browser/relay/relay.html` + `relay.js` — the old test dispatch page was no longer imported or used after all tests were rewritten to use the full MCP JSON-RPC pipeline. Removed from source and from `build.js` copy step.

### Docs updated

**`.github/copilot-instructions.md`** — full rewrite to reflect current state:
- Port `8420 → 8765` throughout
- Removed all references to `src/browser/relay/` (deleted)
- Removed all references to `src/browser/dashboard/` (removed in an earlier session)
- Removed `opendashboard.js` from background module list (never existed)
- Removed `filetourl.js` from common/ (only `sleep.js` exists there)
- Fixed bridge path: `src/server/bridge.js` → `src/server/bridge/bridge.js`
- Fixed OCR path: `src/server/ocr.js` → `src/server/ocr/ocr.js`
- Added `SOCIALMCP_NO_AUTOLAUNCH` env var documentation
- Added `GET /ready` endpoint to bridge description
- Updated plugin manifest fields (removed `css:` and `dashboard:`)
- Updated plugin folder layout (removed `dashboard/` subtree)
- Updated test descriptions to reflect full MCP pipeline (no relay page, no dashboard)
- Updated `post` tool docs: added `dryrun` param, 90 s timeout note

**`README.md`**:
- Port `8420 → 8765` (3 places)
- Removed `relay/` from folder tree and description paragraph
- Fixed `src/server/ocr.js` → `src/server/ocr/ocr.js` in folder tree
- Updated test command: cookies now auto-detected, no need to set env var manually
- Updated port hardcode note

**`docs/plugin-dev-guide.md`**:
- Removed the entire duplicate second half (lines 235–478) — it was an old copy of the guide with stale dashboard content that had been accidentally appended
- Fixed transport section: port `8420 → 8765`, timeout `30 s → 30 s (90 s for post)`

## Why

All three docs had drifted from the code:
- Relay page was removed but still documented everywhere
- Dashboard was removed in a previous session but docs still referenced it
- Port changed from 8420 to 8765 (conflict with another service) but docs weren't updated
- plugin-dev-guide had a full duplicate section with stale dashboard architecture

## Tradeoffs

Rewrote copilot-instructions.md wholesale rather than 20+ surgical edits — cleaner result with less risk of missing scattered stale references.
