# 2025-06-01 19:20 — Real post with image to AKAO Facebook page

## What changed

Ran a **live end-to-end post** via Social MCP: text + image published successfully to the AKAO Facebook page (`https://www.facebook.com/akaoofficial`) directly from the test harness.

Also updated `tests/post.spec.js` to support image attachments via `FACEBOOK_POST_MEDIA` env var.

## Details

### Real post run
- Content: `Social MCP test post 🤖 — automated posting via Chrome extension + MCP server. #socialmcp`
- Media: `tests/assets/test-image.png` (400×300 gradient PNG created at test time)
- Posted via `FACEBOOK_ACTUALLY_POST=true` using the full production pipeline (background `post.js` → content script `switchpage` → `postpage`)
- Result: `{ success: true }` ✓

### `tests/post.spec.js` updates
- Added `FACEBOOK_POST_MEDIA` env var: comma-separated absolute paths to image files
- Media array passed through to `post` params
- Content string now reflects real vs dry-run mode
- Updated inline docs

### `tests/assets/test-image.png`
- 400×300 purple→orange gradient PNG generated with pure Python (no dependencies)
- Used as the test image for the real post

## How to reproduce
```bash
FACEBOOK_ACTUALLY_POST=true \
FACEBOOK_POST_MEDIA=$(pwd)/tests/assets/test-image.png \
FACEBOOK_COOKIES=$(node scripts/extractcookies.js) \
FACEBOOK_POST_PAGE=https://www.facebook.com/akaoofficial \
npm test -- --grep post
```

## Tradeoffs
- Test image is committed to the repo (50 KB) — small enough to be fine; avoids external dependency
- No mock: the post test drives real Facebook via the real extension and real DOM automation
